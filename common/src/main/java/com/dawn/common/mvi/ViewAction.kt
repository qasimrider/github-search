package com.dawn.common.mvi

/**
* MVI(Model-View-Intent) Action.
 *
 * This View Action defines which action should be taken at the time of Intent is changed
 * This is used as base representation of any Action used in the application
 */
interface ViewAction